# 🎬 ربات خبری سینما

ربات تلگرام پیشرفته برای دریافت و ارسال خودکار اخبار سینمایی از منابع مختلف

## ✨ ویژگی‌ها

- 📰 **دریافت خبر از RSS** - پشتیبانی از منابع RSS متعدد
- 🕷️ **Web Scraping** - استخراج خبر مستقیم از سایت‌ها
- 🎯 **دسته‌بندی خودکار** - تقسیم‌بندی اخبار به فیلم، سریال، جشنواره و...
- ⭐ **سطح‌بندی اهمیت** - فیلتر اخبار بر اساس اهمیت (0-3)
- 🌍 **ترجمه خودکار** - ترجمه عناوین به فارسی
- 📊 **تحلیل ترندها** - شناسایی ترندهای روزانه سینما
- 🤖 **پنل مدیریت** - کنترل کامل از طریق تلگرام
- ⏰ **ارسال خودکار** - برنامه‌ریزی ارسال اخبار هر 3 ساعت

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها
- Python 3.9+
- یک ربات تلگرام (از [@BotFather](https://t.me/botfather))
- یک کانال یا گروه برای ارسال اخبار

### نصب سریع

```bash
# 1. کلون کردن پروژه
git clone <repository-url>
cd cinema_news_bot

# 2. نصب کتابخانه‌ها
pip install -r requirements.txt

# 3. تنظیم BOT_TOKEN
export BOT_TOKEN="YOUR_BOT_TOKEN_HERE"

# 4. مقداردهی اولیه منابع (اختیاری)
python initialize.py

# 5. اجرای ربات
python main.py
```

### یا استفاده از اسکریپت خودکار (Linux/Mac)

```bash
chmod +x run.sh
./run.sh
```

## 📁 ساختار پروژه

```
cinema_news_bot/
├── main.py                 # 🚀 فایل اصلی (admin + scheduler)
├── admin_bot.py            # 🤖 پنل مدیریت تلگرام
├── news_scheduler.py       # ⏰ سرویس ارسال خودکار اخبار
├── database.py             # 💾 مدیریت دیتابیس
├── category.py             # 🎯 دسته‌بندی اخبار
├── importance.py           # ⭐ سطح‌بندی اهمیت
├── translation.py          # 🌍 ترجمه متون
├── scrapers.py             # 🕷️ استخراج اخبار
├── trends.py               # 📊 تحلیل ترندها
├── default_sources.py      # 📰 منابع پیش‌فرض
├── initialize.py           # 🔧 مقداردهی اولیه
├── requirements.txt        # 📦 کتابخانه‌ها
├── railway.toml            # 🚂 تنظیمات Railway
└── README.md               # 📖 مستندات
```

## 🎮 نحوه استفاده

### 1️⃣ راه‌اندازی اولیه

```bash
# اجرای ربات
python main.py
```

این فایل دو سرویس را همزمان اجرا می‌کند:
- **پنل ادمین**: برای مدیریت منابع و تنظیمات
- **News Scheduler**: برای ارسال خودکار اخبار

### 2️⃣ پنل مدیریت تلگرام

در تلگرام به ربات `/start` بزنید. منوی زیر ظاهر می‌شود:

```
🎬 پنل مدیریت ربات خبری سینما

➕ افزودن RSS
➕ افزودن Scraping  
❌ حذف منبع
🎯 تنظیم گروه/کانال مقصد
⚙️ مدیریت اهمیت اخبار
```

### 3️⃣ تنظیم کانال مقصد

1. روی "🎯 تنظیم گروه/کانال مقصد" کلیک کنید
2. آیدی عددی کانال را ارسال کنید (مثلاً: `-1001234567890`)
3. ربات یک پیام تست ارسال می‌کند

💡 **نکته**: برای دریافت آیدی کانال:
- ربات را به کانال اضافه کنید
- از ربات [@userinfobot](https://t.me/userinfobot) استفاده کنید

### 4️⃣ افزودن منابع

**منابع RSS:**
- روی "➕ افزودن RSS" کلیک کنید
- آدرس RSS را ارسال کنید (مثلاً: `https://site.com/feed`)

**منابع Scraping:**
- روی "➕ افزودن Scraping" کلیک کنید
- آدرس صفحه را ارسال کنید

### 5️⃣ تنظیم سطح اهمیت

- روی "⚙️ مدیریت اهمیت اخبار" کلیک کنید
- عددی بین 0 تا 3 وارد کنید:
  - **0**: همه اخبار
  - **1**: اخبار معمولی (review, interview)
  - **2**: اخبار مهم (trailer, box office)
  - **3**: اخبار فوری (breaking, Oscar)

## ⚙️ تنظیمات پیشرفته

### تغییر بازه زمانی ارسال اخبار

در فایل `main.py`:

```python
# تغییر از 3 ساعت به 1 ساعت
Thread(target=schedule_news, args=(1,), daemon=True).start()
```

### تغییر ساعت ارسال ترندها

در فایل `main.py`:

```python
# تغییر از 23:55 به 20:00
Thread(target=schedule_daily_trends, args=(20, 0), daemon=True).start()
```

### تغییر آیدی ادمین

در فایل `admin_bot.py`:

```python
ADMIN_ID = YOUR_TELEGRAM_ID  # آیدی عددی خود را وارد کنید
```

💡 برای دریافت آیدی خود از [@userinfobot](https://t.me/userinfobot) استفاده کنید.

## 📊 ویژگی‌های خبررسانی

### دسته‌بندی خودکار
اخبار به این دسته‌ها تقسیم می‌شوند:
- 🎬 فیلم
- 📺 سریال
- 🎭 جشنواره و جوایز
- 👤 بازیگران و عوامل

### سطوح اهمیت
- **سطح 3**: اخبار فوری (مرگ، اسکار، کن)
- **سطح 2**: اخبار مهم (تریلر، باکس آفیس)
- **سطح 1**: اخبار معمولی (نقد، مصاحبه)

### تحلیل ترندها
- اخبار تکراری از منابع مختلف شناسایی می‌شوند
- روزانه ساعت 23:55 ترندهای روز ارسال می‌شود
- حداقل 3 منبع برای تبدیل شدن به ترند لازم است

## 🚂 دیپلوی در Railway

### راه‌اندازی خودکار

1. پروژه را به GitHub بفرستید:
```bash
git add .
git commit -m "Initial commit"
git push
```

2. در [Railway.app](https://railway.app):
   - New Project → Deploy from GitHub
   - پروژه را انتخاب کنید
   - در Variables قسمت:
     - کلید: `BOT_TOKEN`
     - مقدار: توکن ربات شما

3. Deploy خودکار انجام می‌شود! 🎉

### مشاهده لاگ‌ها

```bash
# در Railway dashboard
Deployments → View Logs
```

## 🐛 رفع مشکلات رایج

### ربات پاسخ نمی‌دهد
```bash
# بررسی BOT_TOKEN
echo $BOT_TOKEN

# بررسی آیدی ادمین در admin_bot.py
```

### خطای "BOT_TOKEN not set"
```bash
export BOT_TOKEN="YOUR_TOKEN_HERE"
```

### اخبار ارسال نمی‌شوند
1. مطمئن شوید آیدی کانال تنظیم شده است
2. ربات را admin کانال کنید
3. حتماً منابع RSS یا Scraping اضافه کنید

### خطای import
```bash
# نصب دوباره requirements
pip install -r requirements.txt --upgrade
```

## 📝 فایل‌های داده

دیتابیس به صورت JSON در پوشه `data/` ذخیره می‌شود:

```
data/
├── settings.json    # تنظیمات (TARGET_CHAT_ID, min_importance)
├── sources.json     # منابع RSS و Scraping
├── sent.json        # لیست اخبار ارسال شده
└── topics.json      # ترندهای ذخیره شده
```

## 🔐 امنیت

- ✅ تنها ادمین مجاز به استفاده از پنل است
- ✅ توکن از Environment Variables خوانده می‌شود
- ✅ فایل‌های حساس در `.gitignore` قرار دارند
- ✅ دیتابیس محلی و امن است

## 🤝 مشارکت

برای گزارش باگ یا پیشنهاد:
1. Issue باز کنید
2. یا Pull Request ارسال کنید

## 📄 لایسنس

MIT License - استفاده آزاد برای همه

## 🙏 تشکر

از کتابخانه‌های زیر استفاده شده:
- python-telegram-bot
- feedparser
- beautifulsoup4
- googletrans

---

**ساخته شده با ❤️ برای علاقه‌مندان سینما**
